
%%
for i=1:N % Delete a marker, reconstruct it (fast) and compare

end

%% Select a pair of labels and switch, see which ones we can detect


%% Randomly move markers, test outlier detection

%% Randomly move markers, test reconstruction